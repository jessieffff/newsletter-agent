# newsletter-agent (package)

This package contains the LangGraph workflow and tools.

Entry points:
- `newsletter_agent.workflow.build_graph()` → returns a compiled graph
- `newsletter_agent.workflow.run_once(...)` → convenience helper
