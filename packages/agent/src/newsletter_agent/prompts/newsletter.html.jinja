<!doctype html>
<html>
  <body style="font-family: ui-sans-serif, system-ui; line-height: 1.4;">
    <h2>{{ subject }}</h2>
    <ol>
      {% for it in items %}
      <li style="margin-bottom: 14px;">
        <div><strong>{{ it.title }}</strong> — <em>{{ it.source }}</em></div>
        <div><a href="{{ it.url }}">{{ it.url }}</a></div>
        <div><strong>Why it matters:</strong> {{ it.why_it_matters }}</div>
        <div><strong>Summary:</strong> {{ it.summary }}</div>
      </li>
      {% endfor %}
    </ol>
    <hr/>
    <div style="font-size: 12px; opacity: 0.8;">
      Generated by your Newsletter Agent. Always click through for full context.
    </div>
  </body>
</html>
